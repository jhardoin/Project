document.querySelector('#about-sub').addEventListener('submit', function(event) {
    event.preventDefault(); // Prevent the default form submission behavior
    alert('Thank you for your message');
});